# Path to the claude-plugins marketplace repo, used by `just release` to bump
# this plugin's marketplace entry. Adjust if your checkout lives elsewhere.
export MARKETPLACE_DIR="$HOME/code/claude-plugins"
